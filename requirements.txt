# Core Framework
langgraph>=0.1.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
openai>=1.35.0
aiofiles>=24.1.0

# LLM Integration
tiktoken>=0.7.0

# CLI & Progress
rich>=13.7.0
click>=8.1.7

# Dashboard (live web UI)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=14.0

# Sandbox Dependencies (no Docker required!)
psutil>=6.0.0    # Process/resource monitoring

# Code Quality Tools (in sandbox)
ruff>=0.3.0      # Python linter
# eslint is run via npx in the sandbox, not installed via pip

# Testing
pytest-asyncio>=0.24.0  # Async test support
